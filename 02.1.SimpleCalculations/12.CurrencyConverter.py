amount = float(input('Amount = '))
entry_currency = input('Currency ')
exit_currency = input('Converted currency ')

def convert(amount, currency):
    switcher = {

    }

# converted_amount

# print(converted_amount, exit_currency)